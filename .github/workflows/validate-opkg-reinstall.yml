name: Legacy OpenBH opkg reinstall validation (manual)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy OpenBH reinstall test retained as manual-only; CineView FHD 2.1 official release is OpenATV-only."
